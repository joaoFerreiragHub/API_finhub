# 2026-03-24

- SEC-02 (Security frontend) executado no repo `Front/FinHub-Vite`.
- Implementado: CSP no `server/index.mjs`, sanitização DOMPurify em `ArticleDetailPage`, remoção de `process.env.REACT_APP_*` residual em frontend (`useMLPredictionsRealtime`, `CoursesSection`), shim `src/lib/reactRouterDomCompat.ts` para evitar imports diretos de `react-router-dom` em `src`.
- Checks SEC-02: `npm outdated` inventariado, `npm audit --audit-level=high` com 0 vulnerabilities, `npm run typecheck:p1` PASS, `npm run build` PASS.
- Tracking atualizado em `dcos/finhub/PROMPTS_EXECUCAO.md`: item 68 marcado como ✅ (2026-03-24).
- GDPR-01 concluído: cookie banner com consentimento explícito (`finhub_cookie_consent`), opt-out de analytics com `allowAnalytics` (frontend+backend) e exportação de dados em `GET /api/account/export` com cooldown de 7 dias.
- Tracking atualizado em `dcos/finhub/PROMPTS_EXECUCAO.md`: item 69 marcado como ✅ (Codex — 2026-03-24).
